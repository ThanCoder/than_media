name: than_media
description: "ffmpeg + miniaudio + flutter + dart package.."
version: 0.0.1
repository: https://github.com/ThanCoder/than_media.git

environment:
  sdk: ^3.12.0

dependencies:
  code_assets: ^1.2.1
  hooks: ^2.0.2
  logging: ^1.3.0
  path: ^1.9.1
  ffi: ^2.1.4

dev_dependencies:
  ffigen: ^20.1.1
  flutter_lints: ^6.0.0
  test: ^1.28.0
